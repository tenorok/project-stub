match(!this.data)(function() {

    this.data = {
        bundleName: 'app',
        pageTitle: 'Контакты',
        route: this.ctx.route
    };

    return applyCtx({
        block : 'page'
    });

});
